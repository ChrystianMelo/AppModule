<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.appmodule">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"
        tools:ignore="AllowBackup">
        <activity
            android:name=".utils.SplashScreen"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.HomeActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.SignupActivity" />
        <activity android:name=".view.ProfileActivity" />
        <activity android:name=".utils.DialogProfile" />
        <activity android:name=".utils.FieldVerification" />
        <activity android:name=".repositories.account.SignupRepository" />
        <activity android:name=".repositories.account.ProfileRepository" />
        <activity android:name=".repositories.account.LoginRepository" />
        <activity android:name=".view.SearchActivity" />
        <activity android:name=".utils.NavBottom" />
        <activity android:name=".view.PostActivity" />
        <activity android:name=".repositories.feed.PostRepository" />
    </application>

</manifest>